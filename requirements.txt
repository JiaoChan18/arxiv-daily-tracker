# HTTP 客户端，用于请求 arXiv API
requests==2.32.3

# 解析 arXiv 返回的 Atom feed（XML）
feedparser==6.0.11

# OpenAI Python SDK，通过 base_url 兼容智谱 AI (Zhipu) 接口
openai==2.31.0

# 读取 config.yaml 配置文件
PyYAML==6.0.2

# 从 .env 文件加载密钥到环境变量
python-dotenv==1.1.0

# 纯 Python 轻量调度器，驱动 scheduler.py 的每日定时任务
schedule==1.2.2

# 时区处理，用于 Copenhagen 时间转换
pytz==2025.1
